/* =============================================================================
 * Phia Games — Detail Pages barrel
 * -----------------------------------------------------------------------------
 * Re-exports the per-kind page data and shared types. Consumers should import
 * the merged array + lookup helpers from `src/data/detailPages.ts`.
 * ============================================================================= */

export * from "./detailTypes";
export { projectPages } from "./projectPages";
export { labPages } from "./labPages";
export { articlePages } from "./articlePages";
